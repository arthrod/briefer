export const CONFIG = {
    MANAGER_URL: process.env['MANAGER_URL'],
    IS_MOCK: true,
    ENDPOINTS: {
        LIST: '/api/nodejs/sample-data/list',
        COLUMNS: '/api/nodejs/sample-data/info/preview-fields'
    }
}